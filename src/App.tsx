import React from "react";
import Tree from "./components/Tree";

const App = (): React.ReactElement => {
  return (
    <div className={"App"}>
      <Tree />
    </div>
  );
};

export default App;
